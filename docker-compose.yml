version: '3.8'

services:
  queue:
    build: .
    container_name: job-queue
    ports:
      - "8080:8080"
    volumes:
      - ./data:/app/data  # WAL persistence across restarts
    environment:
      - PORT=8080
      - WAL_DIR=/app/data
    restart: unless-stopped